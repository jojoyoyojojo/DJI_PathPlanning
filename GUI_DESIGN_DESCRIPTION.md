# GUI 设计说明

本文档用通俗语言说明当前 `gui.py` 中桌面界面的设计。这个界面是一个面向 DJI Mavic 3T / M3E 的立面航线规划工具，帮助用户从 4 张带 GPS 信息的立面角点照片生成 DJI 可用的 KMZ 航线文件，以及用于预览的 KML 文件。

## 整体设计目的

当前 GUI 的主要目标是把命令行航线生成流程变成一个可视化、按步骤操作的桌面应用。用户不需要手动记住参数或文件顺序，只需要按界面提示放入 4 张角点照片，检查 GPS 和立面信息，生成航线，然后保存输出文件。

界面采用从上到下的流程设计，共分为 6 个区域：

1. 输入设置
2. 相机与规划设置
3. 飞行设置
4. 图像信息
5. 航线生成
6. 输出

主窗口使用可滚动布局，适合在较小屏幕上显示较多参数。窗口标题为 `Mavic 3T Facade Mission Planner`。

## 1. Input Settings：输入设置

输入设置区是整个工作流的起点，用来让用户指定 4 张立面角点照片和任务基础信息。

### 立面角点拖放网格

界面提供一个 2×2 的角点网格，用户面向墙面时按实际位置放置照片：

- 左上：Top-Left TL
- 右上：Top-Right TR
- 左下：Bottom-Left BL
- 右下：Bottom-Right BR

每个格子可以单独拖入一张图片。支持的图片类型包括 JPG、JPEG 和 DNG。放入图片后，格子里会显示缩略图和文件名，帮助用户确认照片是否放对位置。

这个设计的目的，是把算法需要的角点顺序可视化。算法内部使用的顺序是：

`BL → TL → TR → BR`

也就是左下、左上、右上、右下。界面通过可视化网格降低用户选错顺序的风险。

### 选择图片、选择文件夹、清空

该区域还提供三个按钮：

- `Select Images...`：手动选择 4 张图片，要求按 `BL → TL → TR → BR` 顺序选择。
- `Select Folder...`：选择一个文件夹，程序按文件名字母顺序取前 4 张 JPG/JPEG 图片。
- `Clear All`：清空当前已选择的图片、GPS 信息、生成结果和提示状态。

选择文件夹时，如果图片数量不足 4 张，界面会提示错误；如果超过 4 张，界面会提醒用户程序只使用前 4 张，并建议检查顺序。

### Mission Name

`Mission Name` 用来设置输出任务名称。保存 KMZ 或 KML 时，文件名会使用这个名称。程序保存前会对名称做清理，避免生成非法文件名。

### Distance (m)

`Distance (m)` 表示拍摄角点照片时，相机到立面的距离。它会影响程序从照片 GPS 坐标推算真实立面位置和飞行路径的位置。

这个参数的目的，是告诉程序：照片的位置不是墙面本身，而是离墙面一定距离的相机位置。程序会结合这个距离计算立面平面和航线平面。

## 2. Camera & Planning Settings：相机与规划设置

该区域用于设置无人机型号、相机视场角、照片重叠率，以及航线规划方式。

### Drone Type

`Drone Type` 的可选项来自 `mavic3T_pp_kmz.py` 中的 `DRONE_WPML_PROFILES`。当前默认选择 `M3T`；如果在 profile 中新增其他 DJI WPML 机型，它会自动出现在下拉框中。

选择机型后，程序会自动读取该机型对应的相机参数和 WPML 配置，包括无人机 enum、负载 enum、HFOV 和 VFOV。HFOV 和 VFOV 会根据机型预设自动填入，并在界面上锁定，避免用户误改官方或项目预设的相机视场角。

### HFOV 和 VFOV

`HFOV` 是水平视场角，`VFOV` 是垂直视场角。它们决定单张照片能覆盖多宽、多高的立面区域。

虽然界面显示这两个参数，但在当前设计中，它们会被机型配置锁定。这样做的目的，是让航线间距与所选无人机相机保持一致。

### Overlap (%)

`Overlap (%)` 表示相邻照片之间的重叠率，默认是 65%。界面也提示推荐值不低于 65%。

重叠率越高，生成的航点越密，照片之间覆盖越充分；重叠率越低，航点数量更少，但可能出现覆盖不足。这个参数直接影响航线网格的间距。

### Enable Smart Planning

`Enable Smart Planning` 默认开启。它用于启用更智能的航线规划逻辑，让程序根据立面尺寸和相机覆盖范围自动规划更合适的航点。

这个选项的目的，是减少用户手动调参，让程序根据当前立面自动生成更合理的拍摄路径。

### Force Vertical Plane

`Force Vertical Plane` 默认开启。它会让程序强制把飞行路径规划在一个真正竖直的平面上，即使原始照片因为相机倾斜或 GPS 误差导致拟合平面不完全竖直。

这个功能的目的，是让最终航线更符合建筑立面拍摄需求，避免航线因为数据噪声而出现不必要的倾斜。

## 3. Flight Settings：飞行设置

飞行设置区用于控制无人机执行任务时的基础行为。

### Speed (m/s)

`Speed (m/s)` 设置自动飞行速度，默认是 4.0 m/s。它会写入航线任务中，影响无人机执行任务时的移动速度。

该参数的目的，是让用户根据现场环境、拍摄质量和安全需求调整飞行速度。

### Gimbal Pitch (°)

`Gimbal Pitch (°)` 设置云台俯仰角，默认是 0°。立面拍摄通常需要镜头朝向墙面，因此这个参数会影响每个航点拍摄时的相机朝向。

### Take Photo at Each Waypoint

`Take Photo at Each Waypoint` 默认开启。开启后，程序会在每个航点添加拍照动作，让无人机到达航点后调整云台并拍摄照片。

这个选项的目的，是把“飞到位置”和“拍照”绑定到同一套航线中，减少用户在 DJI 软件中额外配置拍照动作的需要。

## 4. Image Info：图像信息

图像信息区用于显示程序从 4 张角点照片中读取到的 GPS 信息和立面分析结果。

在用户放入照片后，界面会尝试读取每张照片的：

- 纬度
- 经度
- WGS84 高度

显示顺序与角点顺序一致：

- Bottom-Left BL
- Top-Left TL
- Top-Right TR
- Bottom-Right BR

如果某张图片没有 GPS 信息或读取失败，界面会显示错误信息。只有 4 张照片都能成功读取 GPS，并且角点几何检查通过，程序才允许生成航线。

当 GPS 信息有效时，程序还会分析立面几何，并显示估算出的立面宽度和高度，例如：

`Facade: 12.3m × 8.4m`

这个区域的目的，是让用户在生成航线前先确认输入数据是否可信。

## 5. Path Generation：航线生成

航线生成区负责把照片、参数和几何分析结果转换成实际航点。

### Generate Mission

`Generate Mission` 是生成航线的主按钮。它只有在以下条件满足时才会启用：

- 4 个角点格子都已经放入照片
- 4 张照片都有有效 GPS 数据
- 立面角点几何检查通过

点击后，程序会把界面上的参数同步到核心算法模块，然后调用核心逻辑生成航点。

生成成功后，状态区会显示：

- 生成的航点数量
- 使用的蛇形航线方向

例如：

`Status: 24 waypoints generated`

`Direction: horizontal snake pattern`

### 顺序警告

如果程序检测到原始上传顺序可能和推荐角点顺序不一致，会在该区域显示红色警告。

这个警告不会一定阻止生成，因为程序可能已经通过自动几何识别继续处理，但它会提醒用户检查照片是否放错位置，避免立面解释错误或航线方向不符合预期。

### 状态提示

该区域还会显示当前生成状态，例如：

- 等待图片
- 参数已修改，需要重新生成
- 角点几何检查失败
- 生成成功
- 生成出错

这些状态提示的目的，是让用户知道当前下一步应该做什么。

## 6. Output：输出

输出区用于设置保存位置，并导出最终文件。

### Output Directory

`Output Directory` 是输出目录输入框，默认指向用户桌面。用户也可以点击 `Browse...` 选择其他文件夹。

保存前，程序会检查该目录是否存在。如果目录无效，会弹出错误提示。

### Save KMZ

`Save KMZ` 用于保存 DJI 可用的 KMZ 任务文件。该文件内部包含 DJI WPML 所需的：

- `template.kml`
- `waylines.wpml`

这个文件是最终导入 DJI 相关工具或设备执行任务的主要输出。

### Save Preview KML

`Save Preview KML` 用于保存一个普通 KML 预览文件。它主要用于在 Google Earth 等工具中查看航线路径，方便用户检查航线是否覆盖了目标立面。

### Save Both

`Save Both` 会同时保存 KMZ 和预览 KML。

这适合正常工作流：KMZ 用于执行任务，KML 用于人工检查和分享预览。

### 保存状态

保存成功后，界面会显示已保存的文件名；保存失败时会显示错误原因。

## 辅助设计

### DNG 预览支持

界面支持 DNG 图片的缩略图预览。由于部分 Qt 环境不能直接读取 DNG 中的 JPEG 压缩预览，程序内置了一个简单的 TIFF/DNG 解析逻辑，用来提取 DNG 内部最大的 JPEG 预览图。

这个设计的目的，是让用户即使使用 DJI 原始格式照片，也能在角点格子中看到预览图。

### 状态联动

界面会根据当前数据自动启用或禁用按钮：

- 没有 4 张有效照片时，不能生成任务。
- 航线没有生成前，不能保存 KMZ 或 KML。
- 参数变化后，之前生成的航线会失效，需要重新生成。

这种设计可以减少错误操作，避免用户保存过期或无效的任务文件。

### 错误提示

程序在关键步骤提供弹窗或状态文字提示，例如：

- 图片数量不是 4 张
- 文件夹中没有足够图片
- 图片缺少 GPS 信息
- 角点几何检查失败
- 输出目录无效
- 生成或保存失败

这些提示帮助用户快速定位问题，而不是只看到程序没有反应。

## 用户操作流程

典型使用流程如下：

1. 打开 GUI 应用。
2. 将 4 张立面角点照片放入 2×2 网格，或用按钮选择图片/文件夹。
3. 检查图像信息区中的 GPS、立面宽度和高度。
4. 根据需要调整距离、重叠率、飞行速度、云台角度等参数。
5. 点击 `Generate Mission` 生成航点。
6. 检查生成状态中的航点数量和航线方向。
7. 选择输出目录。
8. 保存 KMZ、预览 KML，或同时保存两者。

## 总结

当前 GUI 是一个按任务流程组织的航线生成界面。它把复杂的立面几何计算、航线规划和 DJI 文件生成隐藏在后台，让用户主要关注 4 件事：

1. 正确提供 4 张角点照片。
2. 确认 GPS 和立面信息合理。
3. 设置必要的拍摄和飞行参数。
4. 生成并保存 DJI 可用的任务文件。

整体设计强调顺序清晰、状态可见和错误可提示，适合用于现场任务准备或办公室内的航线预规划。
