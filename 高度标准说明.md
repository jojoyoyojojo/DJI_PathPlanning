# DJI 无人机高度标准详解

## 📊 handdraw.kmz 文件分析结果

### 1. 文件结构
```
handdraw.kmz
├── wpmz/template.kml    (编辑器使用)
└── wpmz/waylines.wpml   (飞行执行使用)
```

### 2. 使用的高度标准

#### 📋 template.kml (编辑器文件)
- **heightMode**: `EGM96`
- **用途**: DJI Pilot 编辑器界面显示
- **高度类型**: 正高 (Orthometric Height) / MSL高度
- **参考面**: 大地水准面（平均海平面）
- **特点**: 这是我们日常理解的"海拔高度"或"绝对高度"

**立面边界点高度 (EGM96)**:
- 点1: 6.65m
- 点2: 7.35m
- 点3: 18.63m
- 点4: 17.95m

#### 🚁 waylines.wpml (执行文件)
- **executeHeightMode**: `WGS84`
- **用途**: 无人机实际飞行时使用
- **高度类型**: 椭球高度 (Ellipsoidal Height)
- **参考面**: WGS84参考椭球面
- **特点**: GPS直接测量值，精度更高

**航点高度 (WGS84)**:
- 最小高度: 7.71m
- 最大高度: 14.09m
- 平均高度: 10.90m
- 总航点数: 14个

---

## 🔄 高度标准对比

### 三种高度类型

| 高度类型 | 英文名称 | 缩写 | 参考面 | 用途 |
|---------|---------|------|--------|------|
| 椭球高度 | Ellipsoidal Height | - | WGS84椭球面 | GPS测量、飞行执行 |
| 正高/海拔 | Orthometric Height | MSL/ASL | 大地水准面 | 日常使用、地图显示 |
| 相对高度 | Relative Height | AGL | 起飞点 | 相对起飞点的高度 |

### 转换关系

```
正高(MSL) = 椭球高度(GPS) - 大地水准面差距(N)

相对高度(AGL) = 椭球高度 - 起飞点椭球高度
```

**香港地区的大地水准面差距**: 约 0-7 米（WGS84椭球面高于平均海平面）

---

## 🔍 您的图像EXIF中的高度

### 检查结果
根据之前的分析，您拍摄的DJI Mavic 3T图像：

```
✓ GPS GPSAltitude (WGS84椭球高度) - 存在
✗ RelativeAltitude (相对于起飞点)  - 不存在
✗ AbsoluteAltitude (MSL海拔高度)   - 不存在
```

**EXIF高度数据**:
- DJI_0109.JPG: 45.05m (WGS84椭球高度)
- DJI_0110.JPG: 78.05m (WGS84椭球高度)
- DJI_0111.JPG: 78.25m (WGS84椭球高度)
- DJI_0112.JPG: 43.85m (WGS84椭球高度)

**转换为MSL高度** (使用精确的geoid模型):
- DJI_0109.JPG: 45.05m MSL (geoid分离值≈0m)
- DJI_0110.JPG: 78.05m MSL
- DJI_0111.JPG: 78.25m MSL
- DJI_0112.JPG: 43.85m MSL

---

## 💡 为什么使用不同的高度标准？

### DJI的设计逻辑

1. **编辑器使用EGM96 (MSL)**
   - ✅ 用户友好：显示真实海拔，容易理解
   - ✅ 与地图一致：Google Earth等地图服务显示的也是MSL
   - ✅ 安全性：便于规划时避开地形障碍物

2. **执行时使用WGS84 (椭球高度)**
   - ✅ GPS精度：GPS直接输出椭球高度，无需转换
   - ✅ 实时性：不需要实时查询geoid模型
   - ✅ 稳定性：避免geoid模型精度或版本差异问题

### 实际飞行中的高度控制

**重要**: DJI无人机在实际飞行时主要依靠：
- **气压计**: 测量相对于起飞点的高度（AGL）- 最精确
- **GPS**: 提供椭球高度作为辅助参考
- **融合算法**: 结合气压计和GPS数据

因此：
- 航点高度（WGS84或EGM96）主要用于路径规划
- 实际飞行高度由气压计相对起飞点测量
- 起飞点的高度决定了整个任务的高度基准

---

## 🛠️ 在您的程序中如何处理

### mavic3T_pp_kmz.py 的高度模式设置

```python
# Height reference modes
# waylines.wpml (execution): "WGS84" or "relativeToStartPoint"
EXECUTE_HEIGHT_MODE = "WGS84"

# template.kml (editor): "EGM96" or "relativeToStartPoint"
TEMPLATE_HEIGHT_MODE = "EGM96"
```

### 建议的处理流程

1. **从EXIF读取高度** → 得到WGS84椭球高度
   ```python
   lat, lon, ellipsoidal_height = read_gps(image_path)
   ```

2. **如果使用WGS84模式**（推荐）
   ```python
   # waylines.wpml 直接使用椭球高度
   execute_height = ellipsoidal_height
   
   # template.kml 转换为EGM96
   template_height = ellipsoidal_height - get_geoid_separation(lat, lon)
   ```

3. **如果使用relativeToStartPoint模式**
   ```python
   # 需要计算相对高度
   relative_height = waypoint_height - takeoff_point_height
   ```

### 使用height_converter.py工具

已为您创建了转换工具：

```bash
python height_converter.py
```

该工具可以：
- 读取图像EXIF中的GPS高度
- 自动计算geoid分离值
- 转换为MSL高度
- 显示详细的分析报告

---

## ⚠️ 注意事项

1. **Geoid分离值的精度**
   - 使用pyproj库可以获得精确的geoid分离值
   - 不同地区的分离值不同
   - 香港地区约0-7米

2. **起飞点高度的重要性**
   - 无人机使用气压计测量相对于起飞点的高度
   - 起飞点的海拔决定了整个任务的绝对高度
   - 即使设置了WGS84或EGM96，实际飞行仍是相对高度

3. **安全建议**
   - 规划任务时充分考虑地形起伏
   - 预留足够的安全高度余量
   - 在复杂地形区域建议使用地形跟随功能

---

## 📚 参考资料

### 相关文档
- DJI WPML 标准文档
- WGS84 椭球模型
- EGM96/EGM2008 大地水准面模型

### 工具
- `height_converter.py` - 高度转换工具
- `mavic3T_pp_kmz.py` - 航点规划主程序
- pyproj库 - 坐标和高度转换

---

**最后更新**: 2025-10-30

