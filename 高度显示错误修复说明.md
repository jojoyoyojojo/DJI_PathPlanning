# 高度显示错误修复说明 (ASL 41.1m 问题)

## 🔴 问题描述

上传 KMZ 到 DJI 平台后，所有航点显示统一的 ASL (Above Sea Level) 高度：**41.1m**

这是错误的！外立面扫描任务的航点应该有**不同的高度**（从低到高扫描墙面）。

---

## 🔍 问题根源

### 对比分析

#### ❌ 修复前的 template.kml

```xml
<!-- 第36行：全局高度设置为平均值 -->
<wpml:globalHeight>41.064830498</wpml:globalHeight>

<!-- 第69行：每个航点都强制使用全局高度 -->
<wpml:useGlobalHeight>1</wpml:useGlobalHeight>

<!-- 航点实际高度（被忽略） -->
<wpml:ellipsoidHeight>28.895567246736</wpml:ellipsoidHeight>  <!-- 28.9m -->
<wpml:ellipsoidHeight>31.035433292412</wpml:ellipsoidHeight>  <!-- 31.0m -->
<wpml:ellipsoidHeight>33.175299338089</wpml:ellipsoidHeight>  <!-- 33.2m -->
...
```

**结果**: DJI 平台忽略了每个航点的实际高度，所有航点统一显示为 41.1m（globalHeight 的值）。

---

## ✅ 解决方案

### 修改内容

**文件**: `mavic3T_pp_kmz.py`  
**行号**: 537

```python
# 修复前
SubElement(pm, "wpml:useGlobalHeight").text = "1"  # ❌ 强制使用全局高度

# 修复后
SubElement(pm, "wpml:useGlobalHeight").text = "0"  # ✅ 使用各自高度
```

### 修复后的效果

```xml
<!-- 现在每个航点使用各自的高度 -->
<wpml:useGlobalHeight>0</wpml:useGlobalHeight>

<!-- 航点1: 28.9m ✓ -->
<wpml:ellipsoidHeight>28.895567246736</wpml:ellipsoidHeight>
<wpml:height>28.895567247</wpml:height>

<!-- 航点2: 31.0m ✓ -->
<wpml:ellipsoidHeight>31.035433292412</wpml:ellipsoidHeight>
<wpml:height>31.035433292</wpml:height>

<!-- 航点3: 33.2m ✓ -->
<wpml:ellipsoidHeight>33.175299338089</wpml:ellipsoidHeight>
<wpml:height>33.175299338</wpml:height>

<!-- 航点递增高度，正确扫描外立面 ✓ -->
```

---

## 📊 修复前后对比

| 项目 | 修复前 | 修复后 |
|------|--------|--------|
| **useGlobalHeight** | `1` (启用) ❌ | `0` (禁用) ✅ |
| **航点1 显示高度** | 41.1m | 28.9m ✅ |
| **航点2 显示高度** | 41.1m | 31.0m ✅ |
| **航点3 显示高度** | 41.1m | 33.2m ✅ |
| **高度变化** | 无变化 ❌ | 递增 ✅ |
| **DJI 平台识别** | 所有点同高度 ❌ | 各点不同高度 ✅ |

---

## 🚀 重新生成 KMZ

### 步骤

```bash
# 1. 激活虚拟环境
cd /Users/andyliu/Documents/宝宝/drone_pp
source drone_env/bin/activate

# 2. 运行修复后的脚本
python3 mavic3T_pp_kmz.py

# 输出:
# Building facade mission...
# Direction: vertical, waypoints: 96
# ✓ Saved preview: Facade Mission_preview.kml
# ✓ Saved KMZ (WPML package): Facade Mission.kmz
```

### 生成的文件

- ✅ **Facade Mission.kmz** - 上传到 DJI 平台（高度已修复）
- ✅ **Facade Mission_preview.kml** - Google Earth 预览

---

## 🔬 验证修复

### 方法1: 检查 useGlobalHeight

```bash
unzip -o "Facade Mission.kmz" -d temp
grep "useGlobalHeight" temp/wpmz/template.kml
```

**期望输出**:
```xml
<wpml:useGlobalHeight>0</wpml:useGlobalHeight>  <!-- ✓ 应该是 0 -->
```

### 方法2: 检查航点高度

```bash
grep -E "<wpml:ellipsoidHeight>" temp/wpmz/template.kml | head -10
```

**期望输出** (高度应该递增):
```xml
<wpml:ellipsoidHeight>28.895567246736</wpml:ellipsoidHeight>  <!-- 28.9m -->
<wpml:ellipsoidHeight>31.035433292412</wpml:ellipsoidHeight>  <!-- 31.0m -->
<wpml:ellipsoidHeight>33.175299338089</wpml:ellipsoidHeight>  <!-- 33.2m -->
<wpml:ellipsoidHeight>35.315165383766</wpml:ellipsoidHeight>  <!-- 35.3m -->
...
```

---

## 📝 技术说明

### useGlobalHeight 的作用

| 值 | 行为 | 适用场景 |
|----|------|----------|
| `1` | 所有航点使用 `globalHeight` 的值 | 平面飞行（同一高度） |
| `0` | 每个航点使用各自的 `ellipsoidHeight` | 立体扫描（不同高度）✅ |

### 外立面扫描特点

- **需要不同高度**: 从下到上扫描墙面
- **蛇形路径**: 水平或垂直交替
- **高度递增**: 每一层比上一层高
- **必须禁用 globalHeight**: 否则所有点会变成同一高度

---

## ✅ 修复完成清单

- [x] 修改代码：`useGlobalHeight` 从 `1` 改为 `0`
- [x] 重新生成 KMZ 文件
- [x] 验证 `useGlobalHeight=0` ✓
- [x] 验证航点高度递增 ✓
- [x] 测试文件：`Facade Mission.kmz`

---

## 🎯 下一步

1. ✅ **上传测试**: 将新生成的 `Facade Mission.kmz` 上传到 DJI 平台
2. ✅ **高度验证**: 确认平台显示的航点高度各不相同（28.9m → 48.1m）
3. ✅ **飞行测试**: 在安全环境下测试实际飞行效果

---

## 📄 相关文件

- `mavic3T_pp_kmz.py` - 主程序（已修复）
- `Facade Mission.kmz` - 生成的任务文件
- `wpmz/template.kml` - 旧文件（对比用）
- `example-kmz/template.kml` - 官方示例（参考用）

---

**修复日期**: 2025-10-30  
**问题**: 所有航点显示统一高度 41.1m  
**原因**: `useGlobalHeight=1` 强制使用全局高度  
**解决**: 改为 `useGlobalHeight=0` 使用各自高度  
**状态**: ✅ 已修复并验证

